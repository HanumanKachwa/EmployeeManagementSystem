# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

workspace 'EMS.xcworkspace'

use_frameworks!
project 'EMS'

target 'EMS' do
    project 'EMS.xcodeproj'

    pod 'Eureka'

end
